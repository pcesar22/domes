#pragma once

/**
 * @file secrets.hpp.example
 * @brief WiFi credentials template for OTA testing
 *
 * Copy this file to secrets.hpp and fill in your credentials.
 * secrets.hpp is gitignored and will not be committed.
 */

namespace domes::secrets {

constexpr const char* kWifiSsid     = "YOUR_WIFI_SSID";
constexpr const char* kWifiPassword = "YOUR_WIFI_PASSWORD";

}  // namespace domes::secrets
