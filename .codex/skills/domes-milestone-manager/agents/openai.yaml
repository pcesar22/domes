interface:
  display_name: "DOMES Program Manager"
  short_description: "Audit DOMES phases, gates, workstreams, and hardware releases"
  default_prompt: "Use $domes-milestone-manager to report the active DOMES phase, parallel workstreams, hardware authorization level, next gate and release, evidence, critical path, risks, CEO decisions, and forecast."
