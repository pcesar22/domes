interface:
  display_name: "DOMES Program Manager"
  short_description: "Select, execute, and audit the next DOMES program result"
  default_prompt: "Use $domes-milestone-manager to reconcile current evidence, select the single highest-priority unblocked AI-owned result, execute it within authorization boundaries, and report the active phase, workstreams, hardware level, next gate, risks, decisions, and forecast."
