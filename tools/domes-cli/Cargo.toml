[package]
name = "domes-cli"
version = "0.1.0"
edition = "2021"
description = "DOMES firmware runtime configuration CLI"
authors = ["DOMES Team"]

[dependencies]
# CLI argument parsing
clap = { version = "4.4", features = ["derive"] }

# Serial port communication
serialport = "4.3"

# CRC32 calculation (matching firmware's IEEE 802.3 polynomial)
crc32fast = "1.4"

# Error handling
anyhow = "1.0"
thiserror = "1.0"

# SHA256 hashing for OTA
sha2 = "0.10"

# Hex formatting for debug output
hex = "0.4"

# Protocol Buffers - generated from firmware/common/proto/*.proto
prost = "0.13"

# BLE support via btleplug
btleplug = "0.11"

# Async runtime for btleplug (tokio)
tokio = { version = "1", features = ["rt-multi-thread", "time", "sync"] }

# Cross-thread channel for notification forwarding
crossbeam-channel = "0.5"

# UUID handling for BLE service/characteristic UUIDs
uuid = { version = "1", features = ["v4"] }

# Async stream processing for BLE notifications
futures = "0.3"

# Logging for debug output
log = "0.4"
env_logger = "0.11"

[build-dependencies]
# Protocol Buffers code generation
prost-build = "0.13"

[dev-dependencies]
# For testing
assert_cmd = "2.0"
predicates = "3.0"

[[bin]]
name = "domes-cli"
path = "src/main.rs"
